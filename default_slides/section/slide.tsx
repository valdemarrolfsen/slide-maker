import { Section } from 'slide-maker/runtime';

export default function SectionSlide() {
  return (
    <Section
      n={2}
      title="Where the time goes"
      lede="Three weeks of instrumentation, and the answer was not the one anybody had bet on."
    />
  );
}
