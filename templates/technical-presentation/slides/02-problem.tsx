import { Slide, Fill, Statement, Note } from 'slide-maker/runtime';
export default function Problem() { return <Slide><Fill><Statement>One slow downstream currently turns every healthy request into a timeout.</Statement></Fill><Note>Production traces · p95 window · Replace with the observed failure mode</Note></Slide>; }
