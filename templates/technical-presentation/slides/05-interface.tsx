import { Slide, Head, Eyebrow, Title, Fill, Columns, Column, Code, Hl, Ticks, Tick } from 'slide-maker/runtime';
export default function Interface() { return <Slide><Head><Eyebrow>Interface</Eyebrow><Title>The safety policy travels with every call</Title></Head><Fill><Columns ratio="1.2fr 1fr" gap={44}><Column><Code>{`const result = await jobs.send(payload, {\n  key: payload.id,\n`}<Hl>{`  budget: { attempts: 4, deadline: '30s' },`}</Hl>{`\n  isolation: tenant.id,\n});`}</Code></Column><Column><Ticks><Tick><b>Idempotent by default.</b> The job key makes a retry safe.</Tick><Tick><b>Budgets are explicit.</b> Attempts and time cannot grow invisibly.</Tick><Tick><b>Isolation is required.</b> Every unit of work has an owning boundary.</Tick></Ticks></Column></Columns></Fill></Slide>; }
